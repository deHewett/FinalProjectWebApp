<!DOCTYPE html>
<html>
    <head>
            <% include ./partials/head %>
    </head>
    <header>
            <% include ./partials/header %>
    </header>
    <body>
        <div id="container">
            <h1>This is all the products!</h1>
            <hr>
        <!--<div>
                <h3>Select Category:</h3>
                <form action="">
                    <input type="checkbox" name="Straw" value="Straw"> Straw<br>
                    <input type="checkbox" name="Recycle Bags" value="Bags"> Bags<br>
                    <input type="checkbox" name="Bottles" value="Bottles" checked> Boottles<br><br>
                </form>
            </div>--> 
            <section class="container">
                <div class="one">
                    <h3>Select a category</h3>
                    <a onclick="">Straws</a>
                    <br>
                    <a onclick="">Bottles</a>
                    <br>
                    <a onclick="">Recycle bag</a>
                </div>
                <div class= "two row" > 
                    <% if(products.length>0){ %>
                        <% for (var i=0; i<products.length; i++) { %>
                                <div class="column">
                                        <img src='<%= products[i].image %>' class="cover-img">
                                            <p><%= products[i].name %></p>
                                            <h3>&dollar;<%= products[i].price %></h3>
                                         <a href='/products/<%= products[i].id %>'><button class="btn">View product</button></a>
                                       
                                    </div>
                            <% }%>
                    <% } else { %>
                        no record found
                        <% } %>
                   
                        
                </div>
            </section>
            <footer>
                    <% include ./partials/footer %>
            </footer>
        </div>
    </body>
</html>